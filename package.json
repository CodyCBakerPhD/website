{
  "name": "resume-site",
  "version": "1.0.0",
  "private": true,
  "description": "Personal website of Cody C. Baker, Ph.D. — static site built with TypeScript (no Jekyll)",
  "type": "module",
  "engines": {
    "node": ">=22.18"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "node scripts/build.ts",
    "serve": "npx --yes serve dist"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
